name = "okx-monitor-01"
main = "src/index.js"
compatibility_date = "2024-01-01"

# After deployment, set these in Cloudflare Dashboard:
#   Workers -> Your Worker -> Settings -> Variables
#   OKX_DEMO = "true" (demo) or "false" (live)
# And set Secrets via: wrangler secret put OKX_API_KEY, etc.
